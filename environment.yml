name: research-rag
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      # Векторная БД
      - qdrant-client>=1.9.0
      # Эмбеддинги (модель deepvk/USER-bge-m3)
      - sentence-transformers>=3.0.0
      # Оркестрация RAG
      - llama-index>=0.10.0
      # GigaChat SDK
      - gigachat>=0.1.0
      # Градио для демо-интерфейса
      - gradio>=4.0.0
      # Эвалуация RAG
      - ragas>=0.1.0
      # Работа с PDF
      - pypdf>=4.0.0
      # Парсинг веб-страниц
      - beautifulsoup4>=4.12.0
      - requests>=2.31.0
      # Утилиты
      - python-dotenv>=1.0.0
      - tqdm>=4.66.0
